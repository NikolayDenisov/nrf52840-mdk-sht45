# nrf52840-mdk-sht45

Проект для работы с датчиком влажности и температуры **Sensirion SHT45** на плате **nRF52840-MDK**.  
Используется нативная работа с TWI (I²C) через регистры, без применения библиотек SDK.


## 📌 Подключение датчика

Датчик SHT45 подключён к выводам микроконтроллера:

```c
#define SCL_PIN 6
#define SDA_PIN 8
```


## 📦 Требования

Перед сборкой необходимо указать пути к SDK и каталогу платы в `Makefile`:

```makefile
MDK_ROOT := /Users/nick/projects/iot/nrf52840-mdk/
SDK_ROOT := /Users/nick/projects/iot/nRF5_SDK_17.1.0_ddde560/
```

Также требуется:

* Установленный **arm-none-eabi-gcc**
* Установленный **arm-none-eabi-binutils**
* Установленный **nrfutil**
* Подключённая по USB плата **nRF52840-MDK**



## 🔧 Сборка проекта

Выполните команду:

```bash
make
```

---

## 🚀 Прошивка устройства

Для прошивки платы:

```bash
make flash
```

Прошивка загружается через встроенный загрузчик nRF52840-MDK.

---

## 🗂 Структура проекта

* `main.c` — инициализация TWI и чтение данных SHT45
* `Makefile` — конфигурация сборки, пути к SDK и плате
* `config/` — конфигурационные файлы проекта
* `drivers/` — драйверы и вспомогательный код

---

